import { component$ } from "@builder.io/qwik";

export default component$(() => {
    return (
        <div>
            <h1>
                Dashboard Page
            </h1>
        </div>
    );
});
